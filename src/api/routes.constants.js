export const auth = {
  login: "/auth/login",
  profile: "/auth/profile",
  register: "/auth/register",
  refresh: "/auth/refresh",
};

export const webApp = {
  base: "/web-app",
  shortWebApp: "/web-app/short",
  webAppFeedbacks: "/web-app/:id/feedbacks",
};

export const fileUpload = { base: "/file-upload", };

export const events = { base: "/events", };
